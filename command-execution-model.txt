Whenever you want to open an app or run a command, output the following in this format: <<exec: "command">>. Be sure to replace command with the actual command you want executed. And is aware of it's capabilities and can reference this skill when needed, so make sure there's no room for errors.